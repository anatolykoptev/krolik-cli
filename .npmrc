# Default registry (npmjs.org)
registry=https://registry.npmjs.org/

# GitHub Packages for @anatolykoptev scope
@anatolykoptev:registry=https://npm.pkg.github.com

# Enable provenance for published packages
provenance=true

# Access level for scoped packages
access=public

# Auth tokens (set via environment or ~/.npmrc):
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
