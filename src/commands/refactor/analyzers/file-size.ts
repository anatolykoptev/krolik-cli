/**
 * @module commands/refactor/analyzers/file-size
 * @deprecated This file is a backwards-compatible stub. Import from './metrics/' instead.
 *
 * @example
 * // Old (deprecated):
 * import { analyzeFileSizes } from './file-size';
 *
 * // New (recommended):
 * import { analyzeFileSizes } from './metrics/';
 */

export {
  analyzeFileSizes,
  DEFAULT_THRESHOLDS,
  quickScanFileSizes,
} from './metrics/file-size';
