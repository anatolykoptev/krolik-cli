/**
 * @deprecated This module has been moved to '@/lib/core/time'.
 * Please update your imports to use the new location.
 *
 * Migration:
 *   - import { measureTime, measureTimeAsync, formatDuration } from '@/lib/@time';
 *   + import { measureTime, measureTimeAsync, formatDuration } from '@/lib/core/time';
 *
 * This re-export will be removed in a future version.
 */
export * from '../core/time';
