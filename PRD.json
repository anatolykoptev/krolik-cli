{
  "version": "1.0",
  "project": "krolik-cli",
  "description": "Test PRD to verify Felix Loop works after refactoring",
  "tasks": [
    {
      "id": "task-1-add-jsdoc",
      "title": "Add JSDoc to severity.ts functions",
      "description": "Add JSDoc documentation to the exported functions in src/types/severity.ts that are missing documentation: priorityToSeverity, severityToPriority, and scoreToConfidenceLevel",
      "complexity": "trivial",
      "priority": "low",
      "acceptance_criteria": [
        "Add JSDoc with @param and @returns to priorityToSeverity function at line 65",
        "Add JSDoc with @param and @returns to severityToPriority function at line 82",
        "Add JSDoc with @param and @returns to scoreToConfidenceLevel function at line 143"
      ],
      "files_affected": ["src/types/severity.ts"],
      "dependencies": [],
      "tags": ["docs", "trivial"],
      "labels": ["documentation"]
    },
    {
      "id": "task-2-extract-constants",
      "title": "Extract magic numbers in todos.ts to constants",
      "description": "Extract hardcoded magic numbers (50, 3, 3) in src/commands/status/todos.ts to named constants at the top of the file",
      "complexity": "simple",
      "priority": "low",
      "acceptance_criteria": [
        "Create constants at top of file: MAX_TODO_LENGTH = 50, MIN_CONTEXT_LINES = 3",
        "Replace magic number 50 at line 140 with MAX_TODO_LENGTH",
        "Replace magic numbers 3 at lines 252 and 255 with MIN_CONTEXT_LINES"
      ],
      "files_affected": ["src/commands/status/todos.ts"],
      "dependencies": [],
      "tags": ["refactor", "simple"],
      "labels": ["cleanup"]
    }
  ],
  "config": {
    "autoCommit": false,
    "runTests": false
  }
}
